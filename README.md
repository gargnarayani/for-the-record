# for-the-record
For the Record is an automated Python pipeline that revives vintage iPods by converting Spotify playlists into metadata-tagged, VBR-compressed offline MP3s. It features multithreading, fuzzy string matching, an SQLite cache, and exponential backoff to handle data smoothly, alongside an integrated option to mirror playlists onto YouTube.
